# Task 8 implementation report

## Approved base

- Approved Task 7 HEAD: `b023c239bd474b68180343afa491d28f668ad705`
- Scope: Task 8 only
- Live calls, model calls, credential access, portal database access, proposal
  execution, CLI/orchestration work, push, and release are excluded.

## TDD evidence

### RED

- Command:
  `node --test skills/ghl-account-audit/tests/publication.test.mjs skills/ghl-account-audit/tests/weekly-memory.test.mjs`
- Node: `v24.13.0`
- Exit code: `1`
- Tests: 20
- Passed: 18
- Failed: 2
- First expected failure:
  `publication rename and projections occur only after verifier pass` failed
  because the existing publisher did not invoke the verifier callback.
- The new Task 8 test module also failed with `ERR_MODULE_NOT_FOUND` for
  `lib/proposals.mjs`, confirming the required implementation was absent.

### GREEN

- Focused command:
  `node --test skills/ghl-account-audit/tests/publication.test.mjs skills/ghl-account-audit/tests/weekly-memory.test.mjs`
- Exit code: `0`
- Tests: 32
- Passed: 32
- Failed: 0
- Full command: `npm --prefix skills/ghl-account-audit test`
- Exit code: `0`
- Tests: 183
- Passed: 183
- Failed: 0

## Verification

- `npm --prefix skills/ghl-account-audit run build`: passed
- `node --check skills/ghl-account-audit/lib/proposals.mjs`: passed
- `node --check skills/ghl-account-audit/lib/verifier.mjs`: passed
- `node --check skills/ghl-account-audit/lib/memory.mjs`: passed
- `node --check skills/ghl-account-audit/lib/report.mjs`: passed
- `git diff --check`: passed
- Generated `ProposalSchema.schema.json` and bundled runtime contracts were
  regenerated by the checked-in build.
- Static verifier import inspection found no adapter, MCP SDK, transport,
  network, model, vault, or repair dependency.
- Publication payloads and the manifest are made read-only before verification.
  The publisher accepts only a passing attestation bound to the exact manifest
  hash and publication root, rechecks the staged payload bytes, and only then
  writes the attestation and renames the publication.
- No live, model, MCP, credential, keychain, portal database, proposal
  execution, CLI/orchestration, scheduler, push, or release operation ran.

## Implemented scope

- Strict proposal schema and compiler for all six approved solution types
- Exact proposal, pack, README, and acceptance-test projections
- Recursive execution-material rejection and object/version/hash validation
- Ordered weekly report with traceable claim/evidence links and separate Grom
  acquisition and onboarding scorecards
- Immutable event writes and deterministic backlog projections
- Approval-hash invalidation and implementation-until-live-verification states
- Closed-directory independent publication verification
- Verifier-before-rename atomic publication gate

## Independent-review hardening pass

Base: `80c52721c7ae33d3ef899a530705917163931b2c`.

RED command:
`node --test skills/ghl-account-audit/tests/weekly-memory.test.mjs`

- Node: `v24.13.0`
- Exit code: `1`
- Tests: 18
- Passed: 13
- Failed: 5
- Expected failures proved the raw-expiry private-data write path, absent
  sampling-universe authentication, normalized execution-material bypass,
  finding-ID backlog duplication, and boolean-only verification promotion.

The hardening pass closed the original sampling limitation by publishing a
sealed, canonical, privacy-safe eligible-universe artifact containing only
opaque references and allowlisted categorical metadata. The verifier now
replays the approved Task 6 selector for census and above-50 stratified samples.

Additional hardening:

- New raw-expiry events use a strict sanitized schema. Historical vault expiry
  events remain readable but cannot be appended through the new writer.
- Mechanism priority and reconciliation are reconstructed through the Task 7
  mechanism module from sealed mechanism facts and hash-bound expert verdicts.
- Proposal eligibility is independently recompiled from the sealed finding,
  current objects, captured versions and hashes, dependencies, and cutoff.
- Grom acquisition and onboarding render independent movement, findings,
  verdict, action-order, and finding-movement subsections.
- The final publication rename reopens the staging directory and every member
  with no-follow semantics and compares device, inode, size, and byte hash.
- Backlog entries deduplicate by stable fingerprint while preserving finding
  aliases and immutable history.
- A matching independent reread receipt with source, evidence, payload hash,
  captured time, and cutoff is required before `VERIFIED`.
- One shared recursive scanner rejects normalized execution fields, executable
  code fences, shell forms, and SDK mutation forms in both compilation and
  verification.

Hardening GREEN:

- Focused publication and weekly-memory tests: 39/39 passed
- Full runtime suite: 190/190 passed
