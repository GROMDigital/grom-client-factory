{
  "name": "grom-client-factory",
  "description": "Grom Digital client setup factory. Skills: client-design (design-doc orchestration, 22 roles + system-guide HTML renderer), ghl-account-audit (read-only live capture/verify/harvest), create-ghl-workflows (drafts-only, delegates to the uxie-ghl-factory engine), reconcile-lp-tracking (advise-only: reconcile a hand-built landing page on disk against the client's tracking design), doctor (environment + prerequisite checks). Landing pages are built outside the factory, directly with the user, then reconciled to tracking.",
  "version": "1.2.0",
  "author": {
    "name": "Grom Digital"
  }
}
